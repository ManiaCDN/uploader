doctrine:
    dbal:
        # For dama/doctrine-test-bundle
        # "Starting from version 8 and only when using DBAL < 4 you need to make sure you have use_savepoints enabled on your doctrine DBAL configuration for all relevant connections"
        connections:
            default:
                use_savepoints: true
